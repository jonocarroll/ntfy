# informative error if topic not set

    Code
      ntfy_topic()
    Condition
      Error in `ntfy_topic()`:
      ! `topic` not set.
      * Either provide `topic` argument.
      * Or set `NTFY_TOPIC` environment variable.

